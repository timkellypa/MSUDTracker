
if (typeof define !== 'function') {
    define = require('amdefine')(module);
}

/*jslint unparam: true */
define(function(require) {
    "use strict";
	var Local;
	/**
	 * Local utilities utilized by the application.
	 * @namespace
	 * @memberof window.Lib
	 */
	Local = {
	};
	return Local;
});