
if (typeof define !== 'function') {
    define = require('amdefine')(module);
}

/*jslint unparam: true */
define(function (require) {
    "use strict";
    var Core;
    /**
     * Contains modules that are used as language extensions
     * along with general reusable objects and patterns
     * @namespace
     * @memberof window
     */
    Core = {};
    return Core;
});