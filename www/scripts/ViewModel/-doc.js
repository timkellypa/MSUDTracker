
if (typeof define !== 'function') {
    define = require('amdefine')(module);
}

/*jslint unparam: true */
define(function(require) {
    "use strict";
	var ViewModel;
	/**
	 * Maintains the state of data objects in a way that is non-UI specific.
	 * State of things are typically observed or altered by widgets.
	 * @namespace
     * @memberof window
	 */
	ViewModel = {
	};
	return ViewModel;
});