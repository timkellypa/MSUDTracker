define = define || null;
if (typeof define !== 'function') {
    define = require('amdefine')(module);
}

/*jslint unparam: true */
define(function(require) {
    "use strict";
	var Classes;
	/**
     * Classes that encapsulate data for our view model.
	 * @namespace
	 * @memberof ViewModel
	 */
	Classes = {
	};
	return Classes;
});