
if (typeof define !== 'function') {
    define = require('amdefine')(module);
}

/*jslint unparam: true */
define(function(require) {
    "use strict";
	var Data;
	/**
	 * Contains data object extensions
	 * for specific data access objects.
	 * @namespace Data
	 * @see Core.DataObject
     * @memberof window
	 */
	Data = {
	};
	return Data;
});