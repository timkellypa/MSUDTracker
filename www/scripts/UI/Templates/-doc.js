
if (typeof define !== 'function') {
    define = require('amdefine')(module);
}

/*jslint unparam: true */
define(function(require) {
    "use strict";
	var Templates;
	/**
	 * HTML templates.  Should contain header extensions (normal header stuff) as well as body.
	 * Don't add a title tag in the header though... that's not really the best place to do that.
	 * @namespace
	 * @memberof window.UI
	 */
	Templates = {
	};
	return Templates;
});