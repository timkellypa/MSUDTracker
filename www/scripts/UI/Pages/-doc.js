
if (typeof define !== 'function') {
    define = require('amdefine')(module);
}

/*jslint unparam: true */
define(function(require) {
    "use strict";
	var Pages;
	/**
	 * Pages should be relatively light functions that compose the app screen using widgets,
	 * hooking those widgets up to items in a view model.
	 * @namespace
	 * @memberof window.UI
	 */
	Pages = {
	};
	return Pages;
});