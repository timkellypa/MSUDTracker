
if (typeof define !== 'function') {
    define = require('amdefine')(module);
}

/*jslint unparam: true */
define(function(require) {
    "use strict";
	var Style;
	/**
	 * Location of stylesheets.  These are typically passed to widgets as header extensions
	 * (e.g. &lt;link rel="stylesheet" href="scripts/UI/Style/DayPickerStyle.css"&gt;)
	 * <br />Header extensions, are also located in "templates" and named "*.header.txt"
	 * @namespace
	 * @memberof window.UI
	 */
	Style= {
	};
	return Style;
});