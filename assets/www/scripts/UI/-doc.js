define = define || null;
if (typeof define !== 'function') {
    define = require('amdefine')(module);
}

/*jslint unparam: true */
define(function(require) {
    "use strict";
    var UI;
    /**
     * UI section of the code. Contains pages and widgets.
     * 
     * @namespace
     * @memberof window
     */
    UI = {
    };
    return UI;
});